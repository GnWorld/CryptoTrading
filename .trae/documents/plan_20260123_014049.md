# 更新TradingWeb前端以连接Admin.NET后端行情接口

## 1. 更新前端行情数据服务

### 1.1 修改marketService.ts

* 更新API基础URL为实际后端地址

* 修改getKLineData函数，调用后端的`/api/Market/candlestick/{symbol}/{interval}`接口

* 修改getMarketSnapshot函数，调用后端的`/api/Market/ticker/{symbol}`接口

* 确保数据格式适配后端返回的结构

### 1.2 修改数据类型定义

* 更新KLineData接口，使其匹配后端Candlestick实体的字段

* 更新MarketSnapshot接口，使其匹配后端FuturesMarketSnapshot模型的字段

## 2. 更新SignalR连接配置

### 2.1 修改useSignalR.ts

* 确保SignalR连接URL正确指向后端的MarketHub

* 确保订阅和取消订阅方法名称与后端一致

* 确保事件名称与后端推送的事件名称一致

## 3. 更新KLine图表组件

### 3.1 修改KLineChart组件

* 确保组件能够正确处理后端返回的K线数据格式

* 调整图表配置，使其更好地展示加密货币行情

## 4. 更新行情展示页面

### 4.1 修改MarketView\.vue

* 确保页面能够正确处理后端返回的行情数据

* 添加更多行情信息展示，如买卖盘数据

* 优化页面布局和用户体验

## 5. 测试功能完整性

### 5.1 测试API调用

* 确保能够成功获取历史K线数据

* 确保能够成功获取单个交易对的行情快照

* 确保能够成功获取所有交易对的行情快照

### 5.2 测试SignalR连接

* 确保能够成功连接到后端SignalR服务

* 确保能够成功订阅单个交易对的行情更新

* 确保能够成功接收实时行情推送

### 5.3 测试价格波动效果

* 确保K线图表能够实时更新

* 确保行情快照能够实时更新

* 确保价格变化时有明显的视觉反馈

## 6. 优化和调整

### 6.1 性能优化

* 添加数据缓存机制，减少不必要的API调用

* 优化图表渲染性能

* 优化SignalR连接管理

### 6.2 用户体验优化

* 添加加载状态指示器

* 添加错误处理和提示

* 优化页面布局和响应式设计

## 7. 部署和验证

### 7.1 构建生产版本

* 运行`npm run build`确保构建成功

* 验证构建产物的正确性

### 7.2 部署到测试环境

* 将构建产物部署到测试环境

* 验证所有功能在测试环境中正常工作

### 7.3 验证与后端的集成

* 确保与后端的所有API接口正常通信

* 确保SignalR实时推送功能正常工作

* 确保数据展示准确无误

